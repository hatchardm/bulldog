# MARKOSPRINT (Booting)

if loc_api erros with #![cfg_attr(feature = "nightly", feature(const_fn))] const_fn removed in loc_api lib line 91 replace with the following:
#![cfg_attr(feature = "nightly", feature(const_fn_trait_bound))]