[workspace]
members = [
    "kernel",
    "kernel-bin",
    "bootloader",
    "boot-proto",
]

resolver = "2"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
lto = false
opt-level = 0
codegen-units = 1


